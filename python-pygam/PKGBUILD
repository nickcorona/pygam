# PKGBUILD generated by pipman
# Python package author: Daniel Serven <dswahpypi@gmail.com>
pkgname=python-pygam
pkgver=0.8.0
pkgrel=1
pkgdesc="GAM toolkit"
arch=(any)
url="https://github.com/dswah/pyGAM"
license=(UNKNOWN)
makedepends=("python" "python-pip")
build() {
  pip install --no-deps --target="pygam" pygam==0.8.0
}
package() {
  sitepackages=$(python -c "import site; print(site.getsitepackages()[0])")
  mkdir -p $pkgdir/"$sitepackages"
  cp -r $srcdir/pygam/* $pkgdir/"$sitepackages"
}